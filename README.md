# WHYFI

> Find out why your internet is acting weird.

WHYFI is a Windows network diagnostic tool that investigates connectivity problems, identifies the most likely failure point, and explains what is happening in plain language.

**One click → investigate → diagnose → explain.**

Instead of making users understand `ping`, DNS, gateways, routing tables, or packet loss, WHYFI gathers the evidence automatically and answers the useful question:

> Why isn't my internet working properly?

---

## What is WHYFI?

Internet problems can happen at very different layers.

Your computer might not be connected. Your router might be unreachable. Your router might work while your ISP is down. DNS might fail even though raw internet connectivity works. Your connection might also technically be online while suffering from severe packet loss.

WHYFI is being built to distinguish those situations automatically.

The goal is not simply to run networking commands.

The goal is to **collect evidence, identify the likely failure point, and tell the user what to do next.**

---

## Current Status

🚧 **Active Development**

WHYFI now has a working Windows diagnostic and diagnosis engine.

It can currently:

- Discover Windows network adapters
- Identify the active primary connection
- Read the preferred Windows default route
- Determine the default gateway
- Test gateway reachability
- Measure gateway latency and packet loss
- Test direct internet reachability using multiple public IP targets
- Inspect the configured DNS resolver
- Compare DNS against alternate resolvers
- Combine measurements into a structured baseline diagnostic
- Produce evidence-backed diagnoses
- Generate confidence scores and recommendations
- Detect unstable connections using packet-loss evidence

Current diagnosis types:

```text
healthy
no_connection
gateway_issue
internet_issue
dns_issue
unstable_connection
unknown
```

---

## Example Diagnosis

```text
Everything looks healthy.
Confidence: 96%

Evidence:
- Primary adapter: Wi-Fi
- Gateway reachable with 0% packet loss
- Public targets reachable: 2/2
- DNS resolution successful

Recommendation:
No action is needed based on the baseline checks.
```

WHYFI's current diagnoses are generated by deterministic rules rather than an AI model.

---

## How It Works

```text
Windows Network
      |
      v
Adapter + Route Discovery
      |
      v
Gateway / Internet / DNS Probes
      |
      v
Baseline Diagnostic Result
      |
      v
Diagnosis Engine
      |
      v
Evidence + Confidence + Recommendation
```

Network measurements remain the source of truth.

An optional AI investigation layer may be added later for ambiguous cases, tool selection, and clearer explanations, but the core diagnostic engine must continue working even when the internet itself is unavailable.

---

## Automated Testing

WHYFI currently has automated pytest coverage for eight diagnostic scenarios:

```text
Healthy connection                         -> HEALTHY
No primary connection                      -> NO_CONNECTION
Gateway + internet unreachable             -> GATEWAY_ISSUE
Gateway healthy + internet unreachable     -> INTERNET_ISSUE
Internet healthy + DNS failure             -> DNS_ISSUE
Significant gateway packet loss            -> UNSTABLE_CONNECTION
Packet loss across multiple public targets -> UNSTABLE_CONNECTION
Only one lossy public target               -> remains HEALTHY
```

Current result:

```text
8 passed
```

Run the test suite with:

```powershell
pytest -v
```

---

## Design Philosophy

WHYFI is intentionally simple on the outside and technically deeper underneath.

The planned application will have:

- No account
- No login
- No cloud database
- No complex dashboard
- No chatbot-style interface
- One primary action: **Diagnose**

The goal is a utility that feels closer to Shazam than a traditional network administration tool.

---

## Technology

### Current

- Python 3.12
- Python dataclasses
- psutil
- dnspython
- Windows PowerShell
- Windows networking utilities
- pytest
- Git
- GitHub

### Planned Desktop App

- Tauri
- React
- TypeScript
- Vite
- Tailwind CSS

### Planned Intelligent Investigation

- Allowlisted diagnostic tools
- Structured AI outputs
- Evidence-backed explanations
- Privacy sanitization
- Constrained tool access

---

## Repository Structure

```text
whyfi/
├── desktop/                  # Future desktop application
├── engine/
│   ├── whyfi/
│   │   ├── diagnostics/
│   │   │   ├── common/
│   │   │   └── windows/
│   │   ├── diagnosis/
│   │   ├── models/
│   │   ├── evidence/
│   │   ├── agent/
│   │   └── privacy/
│   └── tests/
├── docs/
│   ├── architecture/
│   └── screenshots/
├── .github/
│   └── workflows/
├── pyproject.toml
└── README.md
```

---

## Roadmap

### V0.1 — Diagnostic Core

**Status: Mostly implemented**

- [x] Network adapter discovery
- [x] Default-route discovery
- [x] Primary connection identification
- [x] Gateway diagnostics
- [x] Internet reachability
- [x] DNS diagnostics
- [x] Latency measurement
- [x] Packet-loss measurement
- [x] Baseline diagnostic runner
- [ ] Wi-Fi signal-quality diagnostics
- [ ] Local IP/configuration diagnostics
- [ ] More robust latency and packet-loss sampling

### V0.2 — Diagnosis Engine

**Status: In progress**

- [x] Evidence-based diagnosis rules
- [x] Confidence scoring
- [x] Healthy connection detection
- [x] No-connection detection
- [x] Gateway problem detection
- [x] Upstream internet problem detection
- [x] DNS failure detection
- [x] Unstable connection detection
- [x] Simulated failure scenarios
- [x] Automated pytest coverage
- [ ] Additional edge cases
- [ ] Wi-Fi quality reasoning
- [ ] Expanded diagnostic tests

### V0.3 — Desktop Application

**Status: Not started**

- [ ] Tauri application shell
- [ ] React interface
- [ ] One-click Diagnose flow
- [ ] Live diagnostic progress
- [ ] Result screen
- [ ] Technical details view

### V0.4 — Intelligent Investigation

**Status: Not started**

- [ ] AI diagnostic tools
- [ ] Dynamic follow-up investigation
- [ ] Privacy sanitization
- [ ] Human-friendly explanations

### V1.0

- [ ] Windows installer
- [ ] Stable diagnostic engine
- [ ] CI test pipeline
- [ ] GitHub release
- [ ] Documentation
- [ ] Demo video

---

## Development

Create and activate the virtual environment:

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
```

Install WHYFI and development dependencies:

```powershell
pip install -e ".[dev]"
```

Run the test suite:

```powershell
pytest -v
```

---

## Future Ideas

Potential post-V1 features include:

- Jitter analysis
- Loaded latency testing
- Bufferbloat detection
- Speed testing
- VPN detection
- Captive portal detection
- TLS diagnostics
- IPv6 diagnostics
- MTU problem detection
- Advanced traceroute analysis
- Network history
- macOS support
- Linux support

---

## Project Goal

WHYFI is being built as both a practical networking utility and an exploration of how deterministic systems diagnostics can be combined with constrained AI agents.

The objective is **not** to build an AI wrapper around networking commands.

The objective is to build a reliable diagnostic system first, prove its conclusions with measurable evidence and automated tests, and then use AI only where it provides genuine value.